import { Container } from "@mui/system";
import { Typography } from "@mui/material";

export default function IndCohortContent({}) {
    return  <Container sx={{backgrounfColor: "green"}}>
        <Typography> Individual cohort stuff </Typography>
    </Container>

}