import { test, expect } from "e2e/fixtures/testAsAdmin";

test.describe("/api/v1/sections/{id}", () => {
  //PATCH TESTS
  test("tests a patched document in the database", async ({
    request,
    db,
  }) => {});
});
